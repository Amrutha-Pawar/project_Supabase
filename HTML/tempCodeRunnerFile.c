#include<stdio.h>
int main()
{
    int i,numsSize,nums;
    printf("Enter the size of array:");
        scanf("%d",&numsSize);
        printf("Enter the array elements:");
        for(i=0;i<numsSize;i++)
        {
            scanf("%d",&nums);
        }
}